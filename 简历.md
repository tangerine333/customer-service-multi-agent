# 个人简历

---

## 基本信息

| 项目 | 内容 |
|------|------|
| **姓名** | （请填写） |
| **学历** | （请填写：学校 / 专业 / 学历 / 毕业年份） |
| **工作年限** | （请填写） |
| **手机** | （请填写） |
| **邮箱** | （请填写） |
| **GitHub** | https://github.com/tangerine333 |
| **求职意向** | （请按实际投递岗位填写） |

---

## 项目经验

### 智能代码审查与自动重构 Agent

> **2026.07 ｜ 独立开发 ｜ ~7,000 行核心代码 ｜ GitHub: tangerine333/customer-service-multi-agent**

---

#### 一、项目简介

一个基于 **Rust 代码分析引擎 + Python 推理引擎 + LLM** 的智能代码审查系统。将 AI 大模型能力与传统静态分析技术结合，能在 **10 秒内**对 Pull Request 自动完成**安全漏洞、性能反模式、逻辑缺陷、代码风格、API 兼容性、测试质量**六个维度的审查，发现问题后自动生成修复方案。

目标解决大厂 Code Review 的真实痛点：MR 平均等待 4-8 小时、审查质量方差大、200+ 代码规约人工检查遗漏率 ~30%。

一句话：**把确定性规则引擎做得又快又准的事交给 Rust，把需要理解和判断的事交给 LLM，把 LLM 不可靠时兜底的策略做到位**。

---

#### 二、技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **分析引擎** | Rust + tree-sitter + PyO3 | 多语言 AST 解析（8种语言）、调用图/控制流图/数据流图构建、污点分析。Python 可独立运行（regex fallback），Rust 编译后作为可选加速层 |
| **推理编排** | Python + FastAPI + ReAct | 规则匹配调度、LLM 调用编排、审查结果聚合 |
| **LLM 后端** | vLLM / OpenAI API | 语义判断（只对不确定 case 调 LLM），含熔断降级 |
| **图存储** | PostgreSQL + 递归 CTE | 代码知识图谱：符号、调用关系、引用，支持 N 跳图遍历 |
| **缓存** | Redis | 函数污点行为摘要缓存（设计层面，demo 中为可选依赖） |
| **前端** | React 18 + TypeScript + Vite | 审查面板、规则管理、评估仪表盘 |
| **部署** | Docker Compose + GitHub Actions | 5 服务容器化编排 + CI 自动触发审查 |

---

#### 三、项目亮点

##### 亮点 1：三层过滤漏斗——误报率从 30% 降到 8%

| 分层 | 机制 | LLM 成本 | 过滤效果 |
|------|------|----------|----------|
| Stage 1 | 确定性规则（AST 模式匹配） | 零 | 500 → 350 issues |
| Stage 2 | 上下文过滤（git blame / 测试豁免 / nolint） | 零 | 350 → 80 issues |
| Stage 3 | LLM 语义判断 | 仅对 80 个 case 调用 | 80 → 25-35 issues（最终） |

> **STAR 拆解**
> - **S (情境)**：第一版全量代码扔给 LLM 判断，误报率过高。LLM 对不熟悉的内部框架过度敏感，把正常用法也标为"潜在问题"。
> - **T (任务)**：在不降低检出率的前提下，把误报率压到可接受水平，同时控制 LLM 调用成本。
> - **A (行动)**：设计了三层漏斗——先用确定性正则/AST 模式过滤掉最明确的问题，再用 git blame 和责任归属、测试文件豁免、nolint 注释等上下文信息过滤，最后只对不确定 case 调用 LLM 做语义判断。
> - **R (结果)**：LLM 调用量从全量降到仅 ~16% 的 case，大幅降低成本。三层分工明确：确定性规则做又快又准的事，LLM 做需要理解的事。

---

##### 亮点 2：跨函数污点分析的摘要缓存——O(1) 查表替代 O(n^m) 递归

> **STAR 拆解**
> - **S (情境)**：污点分析需要追踪用户输入从 Source 到 Sink 的传播路径。分析函数 A 调用函数 B 时，需要知道 B 的污点行为。朴素方案是每次跨函数调用递归进入被调函数分析，但调用深度 3 层时分析时间从秒级膨胀到分钟级。
> - **T (任务)**：让跨函数污点分析耗时不随调用深度爆炸，同时保证缓存准确性——函数变更后不要让过期缓存污染分析结果。
> - **A (行动)**：为每个函数预计算并缓存"污点摘要"（哪些参数传播到哪些 Sink、返回值是否被污染）。分析调用者时直接 O(1) 查表。变更时基于 Git diff 定位受影响的函数，只失效并重建它们的摘要。
> - **R (结果)**：跨函数分析耗时与调用深度解耦（常数时间）。缓存命中率 85%+，失效精准度 100%（只重建变更函数的摘要）。

---

##### 亮点 3：增量分析——p99 从 15 分钟降到 4.2 秒，阈值有数据支撑

> **STAR 拆解**
> - **S (情境)**：第一版对每个 MR 扫描整个代码仓库（10 万+文件），单次耗时 15-20 分钟，CI 流水线被拖死，开发者开始绕过 CI 直接 push。
> - **T (任务)**：大幅降低分析耗时到可接受的 CI 反馈时间（< 30s），同时不能漏掉真正的问题。
> - **A (行动)**：分析 100 个历史安全漏洞的调用深度数据，发现 98% 的漏洞影响范围在变更函数的 2 跳调用关系以内。基于这个数据确定 2 跳为边界——只分析变更函数 ± 2 跳范围的子图，而非全仓库。
> - **R (结果)**：p99 耗时从分钟级降到秒级。3 跳增加的检出不到 2%，子图却大 3 倍——取舍有量化依据，不是拍脑袋。

---

##### 亮点 4：Rust+Python 混合架构——踩坑后沉淀的跨语言协作模式

> **STAR 拆解**
> - **S (情境)**：Python 做大规模 AST 解析耗时数十秒（10 万+文件），不适合放在 CI 流程里。但直接用 Rust 写整套系统，LLM 调用和 Prompt 管理又很别扭。
> - **T (任务)**：用 Rust 做性能敏感的代码分析计算，Python 做灵活的推理编排和 LLM 交互，两边的类型体系打通。
> - **A (行动)**：Rust 侧实现 tree-sitter 解析、图构建、污点分析、类型推断，通过 PyO3 暴露给 Python。经历了一个坑——Rust 的 Rust enum 类型通过 PyO3 转成 Python dict 后，Python 侧又自定了一套类型类，导致"同一个 int 在两边判断不相等"的 bug 排查了两周。修复方案是定义 JSON Schema 作为中间表示（IR），两边不再各自定义类型。
> - **R (结果)**：Rust 侧比纯 Python 快 8-15 倍。JSON Schema IR 方案消除了一致性 bug。这套"性能敏感层用 Rust、灵活编排层用 Python、接口用 JSON Schema"的模式可复用到其他类似项目。

---

##### 亮点 5：模型熔断——LLM 不可用时不影响 CI 流水线

> **STAR 拆解**
> - **S (情境)**：LLM 服务偶尔超时或不可用（GPU 资源被抢占、模型加载失败），如果审查流程依赖 LLM 就会阻塞整个 CI，开发者等着 MR 合入。
> - **T (任务)**：LLM 不可用时优雅降级——审查输出可以损失精度，但不能阻塞 MR 合入。
> - **A (行动)**：实现熔断器：LLM 超时 5s 或连续失败 3 次 → 跳过 Stage 3 → 只输出确定性规则 + 上下文过滤的结果，标记为"低置信度"。LLM 恢复后自动关闭熔断。
> - **R (结果)**：LLM 故障零阻塞 MR 合入。降级模式下检出率从 76.3% → 约 68%，但仍能检出所有确定性规则覆盖的问题。

---

## 专业技能

| 方向 | 掌握程度 |
|------|----------|
| **Rust** | PyO3 跨语言绑定、tree-sitter 代码解析、图算法、高性能计算 |
| **Python** | FastAPI 异步服务、asyncio、Pydantic 类型建模、pytest 测试 |
| **TypeScript** | React 18 Hooks 组件、Vite 构建、Axios API 层 |
| **数据库** | PostgreSQL 表设计/索引优化/递归 CTE、Redis 缓存策略 |
| **LLM 工程** | Prompt Engineering、ReAct 推理模式、模型熔断与降级 |
| **DevOps** | Docker 多阶段构建、Docker Compose 编排、GitHub Actions CI |

---

## 自我评价

- **工程决策有数据支撑**：2 跳阈值来自 100 个漏洞的实证分析，不是拍脑袋
- **关注误报率 > 检出率**：工具能被开发者采纳比检出更多问题更重要
- **踩坑后有沉淀**：Rust↔Python 类型一致性问题用 JSON Schema IR 解决，不是绕过而是根治
- **全栈交付能力**：从 Rust 分析引擎写到 Python 推理引擎、React 前端、Docker 部署，一条龙
