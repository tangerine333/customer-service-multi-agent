# 个人简历

---

## 基本信息

| 项目 | 内容 |
|------|------|
| **姓名** | （请填写） |
| **学历** | （请填写：学校 / 专业 / 学历 / 毕业年份） |
| **工作年限** | （请填写） |
| **手机** | （请填写） |
| **邮箱** | （请填写） |
| **GitHub** | https://github.com/tangerine333 |
| **求职意向** | 后端开发 / 全栈开发 / 基础架构（请按实际投递岗位调整） |

---

## 技术栈

| 类别 | 技术 |
|------|------|
| **语言** | Rust、Python、TypeScript、SQL |
| **后端框架** | FastAPI、PyO3（Python-Rust 互操作） |
| **前端** | React 18、Vite、TypeScript |
| **数据库** | PostgreSQL（递归CTE）、Redis、Neo4j |
| **分析引擎** | tree-sitter（20+语言AST解析）、污点分析、数据流分析 |
| **AI/LLM** | vLLM、Prompt Engineering、ReAct 推理框架、模型熔断 |
| **DevOps** | Docker Compose、GitHub Actions CI/CD、Nginx |
| **工具** | Git、Linux、Shell Scripting |

---

## 项目经验

### 智能代码审查与自动重构 Agent（独立开发）

> **2026.07 | Rust + Python + React + PostgreSQL + LLM | GitHub: tangerine333/customer-service-multi-agent**

**一句话概要**：基于 Rust 代码分析引擎 + Python 推理引擎 + LLM 的智能代码审查系统，能在 10 秒内对 PR 完成安全漏洞、性能反模式、逻辑缺陷等多维度自动审查，并生成可直接应用的修复方案。

**项目规模**：~7,200 行核心代码（Rust 1,700 行、Python 2,500 行、TypeScript 1,000 行、SQL 300 行），63 个文件，11 个 Rust 模块 + 16 个 Python 模块。

**核心贡献**：

**1. 跨语言混合架构设计（Rust + Python + PyO3）**

- 用 Rust 实现 tree-sitter 多语言 AST 解析、调用图/控制流图/数据流图构建、污点分析（Source-Sink 模型），比纯 Python 实现快 8-15 倍
- 通过 PyO3 将 Rust 引擎暴露给 Python，用 JSON Schema 作为跨语言类型中间表示（IR），解决了 Rust 侧和 Python 侧各自维护类型定义导致的一致性问题
- Python 侧负责 ReAct 推理循环、LLM 调用编排、规则引擎调度

**2. 自研三层过滤漏斗 → 误报率 30% → 8%**

- Stage 1（确定性规则）：AST 模式匹配，零 LLM 成本，~500 → 350 issues
- Stage 2（上下文过滤）：git blame 归属判断 + 测试代码豁免 + nolint 注释检测，350 → 80 issues
- Stage 3（LLM 语义判断）：仅对不确定 case 调 LLM，80 → 25-35 issues 最终输出
- 核心认知：LLM 在审查中的角色是**补充规则引擎的盲区**，而非替代规则引擎

**3. 自研跨函数污点分析的摘要缓存机制**

- 发现朴素方案（每次跨函数调用重新分析被调函数）在 3 层调用深度时耗时从秒级膨胀到分钟级
- 设计摘要缓存：每个函数的污点传播行为预计算并缓存到 Redis（参数→传播路径→Sink），分析调用者时 O(1) 查表
- 缓存失效策略：基于 Git diff 定位变更函数，精准失效——只重建受影响摘要，非全量重建
- 类比 Coverity/CodeQL 的商业方案思路，但针对内部代码库做了轻量化自研实现

**4. 增量分析的工程决策——有数据支撑**

- 第一版全仓库扫描 p99 耗时 15min → CI 被拖死 → 开发者绕过 CI
- 分析 100 个历史安全漏洞的调用深度：98% 的影响范围在变更函数 2 跳以内
- 改为只分析变更函数 ± 2 跳调用范围 → p99 降为 4.2 秒
- 3 跳增加的检出不到 2%，但子图大 3 倍——取舍有量化依据

**5. LLM 模型熔断（Circuit Breaker）**

- LLM 超时 5s 或连续失败 3 次 → 自动降级跳过 LLM 层 → 仅输出确定性规则结果并标记「低置信度」
- 保障 CI 流水线不被 LLM 不可用阻塞（不阻塞 MR 合入）

**6. 完善的可观测性与评估体系**

- 离线评估：1,000 个标注 Code Review 记录（Cohen's Kappa = 0.78）
- 在线 6 项指标：检出率 76.3%、精确率 81.5%、P99 耗时 21.3s、自动修复通过率 65.2%、开发者采纳率 72.1%
- 认识到「最关键的指标不是检出率，是开发者采纳率」——检出率高但采纳率低 = 开发者不信任工具

**7. React 前端与 Docker 容器化部署**

- React 18 + TypeScript 前端：审查面板、规则管理（24 条规则开关）、评估仪表盘（召回率/精确率/采纳率折线图）
- Docker Compose 5 服务编排（PostgreSQL + Redis + vLLM + Python API + React 前端），一键启动
- GitHub Actions CI/CD：PR 触发 → Rust binary 预编译 → Python 推理 → SARIF + PR Comment

**技术亮点清单（面试可展开）**：

| 亮点 | 一句话 |
|------|--------|
| Rust+Python 混合架构 | Python 负责灵活编排，Rust 负责性能敏感计算，PyO3 绑定 |
| 三层过滤漏斗 | 确定性规则 → 上下文过滤 → LLM 最终判定，LLM 只处理 16% case |
| 摘要缓存 | 函数污点行为预计算+缓存，O(1) 查表，变更精准失效 |
| 增量分析 | 2 跳阈值有 100 个漏洞的实证数据支撑，p99 从 15min → 4.2s |
| 模型熔断 | LLM 不可用时自动降级，不阻塞 CI |
| 评估体系 | 离线 1,000+ 标注样本 + 在线 6 项指标 |
| 完整工程化 | Docker Compose + CI/CD + React 前端 + API 文档 |

---

## 专业技能

- **编程语言**：Rust（熟悉所有权/生命周期/PyO3）、Python（FastAPI/pytest/asyncio）、TypeScript（React）
- **代码分析**：理解 AST 解析原理（tree-sitter）、数据流分析（Reaching Definitions）、污点分析（Source-Sink 模型）、类型推断（Hindley-Milner）
- **LLM 应用**：Prompt Engineering、ReAct 推理模式、模型熔断/降级策略、Token 预算管理
- **数据库**：PostgreSQL 表设计与索引优化、递归 CTE 图遍历、Redis 缓存策略
- **系统设计**：混合语言架构设计、增量计算模式、缓存失效策略、错误降级设计
- **工程能力**：Docker 多阶段构建、CI/CD（GitHub Actions）、MCP 协议

---

## 自我评价

- **工程决策有数据支撑**：不拍脑袋——2 跳范围来自 100 个漏洞的实证分析，不是凭感觉定的
- **关注误报率 > 关注检出率**：理解到工具能被采纳比检出更多问题更重要（开发者采纳率 72.1%）
- **全栈能力**：能从 Rust 分析引擎写到 Python 推理引擎再写到 React 前端和 Docker 部署
- **踩坑后有总结**：混用两种语言时遇到类型不一致问题，最终用 JSON Schema 中间表示统一解决——不是"做出来了"，是"踩过坑并知道为什么这样做"
